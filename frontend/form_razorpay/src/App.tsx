/**
 * App.tsx — kept for backward-compatibility.
 * All routing is handled in main.tsx.
 * Business logic lives in src/hooks/useBookingForm.ts
 * API calls live in src/services/api.ts
 * Types live in src/types/booking.ts
 */
export { default } from "@/pages/BookingPage";
